using GameJam.Assets.Math;
using System;
using System.Diagnostics;

namespace GameJam
{
    class Program
    {
        static void Main()
        {
            //ValueNoise n = new ValueNoise();
            //for(int y = 0; y < 2; y++)
            //    for (float x = 0; x < 2; x += 0.1f)
            //        Debug.Print("Number: " + x + " Value: " + n.Get(x));
            Game gameInstance = new Game();
        }
    }
}
